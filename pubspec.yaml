name: sonic_ether
description: Reproductor Avanzado de Audio
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.5
  just_audio: ^0.10.5
  supabase_flutter: ^2.12.4
  flutter_dotenv: ^6.0.0
  http: ^1.6.0
  audio_session: ^0.1.18

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/bustamante.jpg
    - assets/images/ruggero.jpg
    - assets/images/ruslana.jpg
    - .env